const mongoose = require('mongoose')
const Schema = mongoose.Schema

const userDispatch = new Schema(
    {
        "user id": { type: String, required: true }
    },
    { timestamps: true },
)

module.exports = mongoose.model('userKeyData', userDispatch)
